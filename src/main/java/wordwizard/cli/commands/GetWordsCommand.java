package wordwizard.cli.commands;

import wordwizard.service.CentralService;

public class GetWordsCommand extends Command{
    protected GetWordsCommand(CentralService mainGuy) {
        super(mainGuy);
    }

    @Override
    public void execute() {

    }
}
